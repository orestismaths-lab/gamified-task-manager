# GitHub Actions Workflow - Disabled
# This project uses Vercel for deployment
# GitHub Pages requires static export which is not compatible with this Next.js App Router setup

name: Deploy to GitHub Pages (Disabled)

on:
  workflow_dispatch:  # Only manual trigger, not on push

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow is disabled."
          echo "This project uses Vercel for deployment."
          echo "GitHub Pages requires static export which is not compatible with Next.js App Router."
          echo "Please use Vercel instead: https://vercel.com"
